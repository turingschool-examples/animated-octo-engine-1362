<% @studios.each do |studio| %>
  <section id="studio-<%= studio.id %>">
    <h1><%= studio.name %></h1>
    <p><%= studio.location %>
    <% studio.movies.each do |movie| %>
      <p><%= movie.title %></p>
      <p><%= movie.creation_year %></p>
      <p><%= movie.genre %></p>
    <% end %>
  </section>
<% end %>