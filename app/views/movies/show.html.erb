<%= @movie.title %></br>
<%= @movie.creation_year %></br>
<%= @movie.genre %></br>
<%= @movie.list_actors %></br>
Average Actor Age: <%= @movie.ave_actor_age %> years

Add an Actor
<%= form_with url: "/movies/#{@movie.id}/actor/#{params[:actor_id]}", method: :post, local: true do |form| %>
  <%= form.label :actor_id %>
  <%= form.number_field :actor_id %>
  <%= form.submit "submit" %>
<% end %>