<%= form_with url: "/movies/#{@movie.id}", method: :post, local: true do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.label :instument %>
  <%= form.text_field :age %>

  <%= form.submit "Create Actor" %>
<% end %>